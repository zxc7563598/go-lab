#!/bin/bash

# 检查参数数量，不足时输出错误信息并以错误码退出
if [ $# -ne 2 ]; then
    echo "错误: 需要两个参数"
    echo "用法: $0 <暂停秒数> <文本内容>"
    exit 1  # 返回错误码1退出
fi

# 直接输出第二个参数，暂停第一个参数指定的秒数
echo "开始打印内容: $2"
sleep "$1"
echo "内容打印结束: $2"